# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Mallette Resource Planning - a Node.js application (currently in early development).

## Commands

- **Run the application**: `node index.js`
- **Run tests**: No test framework configured yet

## Architecture

This is a new project with minimal structure:
- `index.js` - Application entry point
- `package.json` - Node.js package configuration (private package)
